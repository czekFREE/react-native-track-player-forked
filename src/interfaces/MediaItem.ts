export type MediaItem = {
            mediaId: string
            title: string
            subtitle: string
            description: string
            mediaUri: string
            iconUri: string
            playable: boolean
}
